    #spring.datasource.url=jdbc:oracle:thin:@//localhost:1521/omsdb
    #spring.datasource.username=omsuser
    #spring.datasource.password=omspass
    #spring.datasource.driver-class-name=oracle.jdbc.OracleDriver
    spring.thymeleaf.prefix=classpath:/templates/
    spring.thymeleaf.suffix=.html
    spring.thymeleaf.mode=HTML
    spring.thymeleaf.encoding=UTF-8
    spring.thymeleaf.cache=false

    # OMS Service Configuration
    oms.base-url=https://your-oms-instance.com/api
    oms.auth-url=https://your-oms-instance.com/auth
    oms.username=Moganapreya.s@titan.co.in
    oms.password=Dragonfruit#27
    oms.client-id=your-MYP1y90MJmI1cKfD3jFZcVR6LPRUJYUw
    oms.client-secret=SieXeRNQuBFulYsHplTbR6Kil6MYO1qY

    # Titan Order Count Service Configuration (IBM Sterling OMS)
    titan.order-count.url=https://titan-preprod-1.oms.supply-chain.ibm.com/smcfs/restapi/executeFlow/Titan_OrderCount

    # Titan Manual Cancellation Service Configuration
    titan.manual-cancellation.url=https://titan-preprod-1.oms.supply-chain.ibm.com/smcfs/restapi/executeFlow/Titan_ManualCancellation

    # Titan Backorder Service Configuration
    titan.backorder.url=https://titan-preprod-1.oms.supply-chain.ibm.com/smcfs/restapi/executeFlow/Titan_Backorder

    # Titan Rejected at POS Service Configuration
    titan.rejected-at-pos.url=https://titan-preprod-1.oms.supply-chain.ibm.com/smcfs/restapi/executeFlow/Titan_RejectedAtPoss

    # Titan Fraud Hold Orders Service Configuration
    titan.fraud-hold.url=https://titan-preprod-1.oms.supply-chain.ibm.com/smcfs/restapi/executeFlow/Titan_FraudHold_Orders

    # Titan Stuck Order Service Configuration
    titan.stuck-order.url=https://titan-preprod-1.oms.supply-chain.ibm.com/smcfs/restapi/executeFlow/Titan_Stuck_Order

    # Titan HMTR (MTR) Orders Service Configuration
    titan.hmtr-orders.url=https://titan-preprod-1.oms.supply-chain.ibm.com/smcfs/restapi/executeFlow/Titan_MTR_Orders

    # Titan EGC/GC Orders Service Configuration
    titan.egc-gc-order.url=https://titan-preprod-1.oms.supply-chain.ibm.com/smcfs/restapi/executeFlow/Titan_EGC_GC_Order

    # Sterling Login / Token Configuration
    titan.login.url=https://titan-preprod-1.oms.supply-chain.ibm.com/smcfs/restapi/invoke/login
    titan.login.id=admin
    titan.login.password=Qwerty
    # Token expiry in minutes (Sterling default session ~30 min; keep slightly below)
    titan.token.expiry-minutes=25

    # SSL truststore configuration for Titan preprod certificate
    # Path can be classpath: or file: location, or absolute filesystem path.
    # Example: titan.truststore.path=file:/path/to/titan-preprod-truststore.jks
    # Example: titan.truststore.password=changeit
    # Use the provided preprod PKCS12 key:
    titan.ssl.truststore-path=classpath:titan-truststore.p12
    # Truststore password for the preprod key
    titan.ssl.truststore-password=ibmkey

    titan.ssl.keystore-path=classpath:titan-keystore.jks
    titan.ssl.keystore-password=ibmkey